---
import { Image } from 'astro:assets';
import BaseLayout from '@layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import { url } from '@utils/url';
import { getProjectImage } from '@utils/images';
import profileImage from '@assets/images/profile.jpg';

// Get featured content
const allSkills = await getCollection('skills');
const allProjects = await getCollection('projects');

const featuredSkills = allSkills.slice(0, 4);
const featuredProjects = allProjects.slice(0, 3);
---

<BaseLayout title="Accueil">
  <!-- Hero Section -->
  <section class="min-h-[90vh] flex items-center relative overflow-hidden py-20 md:py-32">
    <div class="container-custom">
      <div class="grid lg:grid-cols-2 gap-16 items-center">
        <!-- Text Content -->
        <div data-gsap="hero">
          <p
            class="text-primary-400 font-heading font-semibold mb-6 uppercase tracking-wider text-sm"
          >
            Bienvenue sur mon portfolio
          </p>
          <h1 class="page-title text-balance">
            Bonjour, je suis <span
              class="gradient-text-animated underline-zigzag"
              data-text="Virgil">Virgil CHAREYRE</span
            >
          </h1>
          <p class="text-3xl md:text-4xl text-accent-400 mb-6 font-heading font-bold">
            <span class="highlight-marker highlight-marker-accent">Développeur Web Fullstack</span>
          </p>
          <p class="text-gray-300 text-xl mb-10 max-w-xl leading-relaxed">
            Le craft et le souci du détail, c'est ce qui me drive. Je crée des expériences web
            soignées et accessibles — parce que le web mérite mieux que du vite-fait.
          </p>

          <!-- CTA Buttons - Bold Memphis -->
          <div class="flex flex-wrap gap-4">
            <a href={url('/projects')} class="btn-chunky-primary">
              Voir mes réalisations
              <svg
                class="w-6 h-6"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                aria-hidden="true"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
              </svg>
            </a>
            <a href={url('/contact')} class="btn-chunky-accent"> Me contacter </a>
          </div>

          <!-- Quick Stats - Bold Memphis with subtle variations -->
          <div class="mt-12 sm:mt-16 flex flex-wrap justify-start gap-6 sm:gap-10 md:gap-16">
            <div class="text-center group">
              <p class="stat-large text-primary-400">10</p>
              <p
                class="text-xs sm:text-sm text-gray-400 font-heading font-medium uppercase tracking-wide mt-2"
              >
                Compétences
              </p>
            </div>
            <div class="text-center group">
              <p class="stat-large text-accent-400">5</p>
              <p
                class="text-xs sm:text-sm text-gray-400 font-heading font-medium uppercase tracking-wide mt-2"
              >
                Réalisations
              </p>
            </div>
            <div class="text-center group">
              <p class="stat-large text-white">3</p>
              <p
                class="text-xs sm:text-sm text-gray-400 font-heading font-medium uppercase tracking-wide mt-2"
              >
                Années d'exp.
              </p>
            </div>
          </div>
        </div>

        <!-- Hero Image with enhanced styling + depth -->
        <div class="hidden lg:block min-h-[320px]" data-gsap="reveal">
          <div
            class="w-80 h-80 mx-auto overflow-hidden shadow-hard-purple-xl border-3 border-primary-500 bg-dark-800 relative group transition-all duration-500"
          >
            <div class="w-full h-full flex items-center justify-center overflow-hidden">
              <Image
                src={profileImage}
                alt="Photo de Virgil CHAREYRE"
                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                width={320}
                height={320}
                loading="eager"
                fetchpriority="high"
                format="webp"
                quality={85}
              />
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Skills Section -->
  <section
    class="section-padding section-alt relative overflow-hidden section-lazy"
    aria-labelledby="skills-heading"
  >
    <div class="container-custom">
      <div class="section-header" data-gsap="reveal">
        <h2 id="skills-heading" class="section-title">
          Mes <span class="text-primary-400 underline-memphis">Compétences</span> Clés
        </h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">
          Du technique et de l'humain, parce que l'un ne va pas sans l'autre.
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 md:gap-8" data-gsap="stagger">
        {
          featuredSkills.map((skill) => {
            return (
              <a href={url(`/skills/${skill.id}`)} class="card-glow p-4 sm:p-6 md:p-8 group">
                <div
                  class="icon-box mb-6 transition-shadow duration-300"
                  transition:name={`skill-icon-${skill.id}`}
                >
                  <span class="text-2xl">{skill.data.icon || '✦'}</span>
                </div>
                <h3
                  class="text-xl font-heading font-bold text-white mb-3 group-hover:text-primary-400 transition-all"
                  transition:name={`skill-title-${skill.id}`}
                >
                  {skill.data.title}
                </h3>
                <p class="text-gray-400 text-sm line-clamp-2 mb-6">{skill.data.summary}</p>
                <div class="skill-level-animated">
                  <div class="skill-fill" style={`width: ${skill.data.level * 10}%`} />
                </div>
              </a>
            );
          })
        }
      </div>

      <div class="text-center mt-16">
        <a href={url('/skills')} class="btn-secondary">
          Voir toutes mes compétences
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Featured Projects Section -->
  <section
    class="section-padding relative overflow-hidden section-lazy"
    aria-labelledby="projects-heading"
  >
    <div class="container-custom">
      <div class="section-header" data-gsap="reveal">
        <h2 id="projects-heading" class="section-title">
          Réalisations <span class="text-accent-400 underline-memphis underline-memphis-accent"
            >Récentes</span
          >
        </h2>
        <div class="section-divider section-divider-accent"></div>
        <p class="section-subtitle">
          Ce sur quoi j'ai bossé ces dernières années, en entreprise et en perso.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-4 sm:gap-6 md:gap-8" data-gsap="stagger">
        {
          featuredProjects.map((project) => {
            return (
              <a href={url(`/projects/${project.id}`)} class="card-glow overflow-hidden group">
                <div
                  class="aspect-video bg-dark-700 relative overflow-hidden"
                  transition:name={`project-thumb-${project.id}`}
                >
                  {project.data.thumbnail && getProjectImage(project.data.thumbnail) ? (
                    <Image
                      src={getProjectImage(project.data.thumbnail)!}
                      alt={`Aperçu du projet ${project.data.title}`}
                      class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                      loading="lazy"
                      decoding="async"
                      width={384}
                      height={216}
                    />
                  ) : (
                    <img
                      src={url(project.data.thumbnail || '/images/project-placeholder.jpg')}
                      alt={`Aperçu du projet ${project.data.title}`}
                      class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                      loading="lazy"
                      decoding="async"
                      width="384"
                      height="216"
                    />
                  )}
                  {/* Gradient overlay on hover */}
                  <div class="absolute inset-0 bg-gradient-to-t from-primary-500/30 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                </div>
                <div class="p-4 sm:p-6 md:p-8">
                  <h3
                    class="text-xl font-heading font-bold text-white mb-3 group-hover:text-primary-400 transition-all"
                    transition:name={`project-title-${project.id}`}
                  >
                    {project.data.title}
                  </h3>
                  <p class="text-gray-400 text-sm line-clamp-2 mb-6">
                    {project.data.shortDescription}
                  </p>
                  <span class="link-arrow" aria-hidden="true">
                    Voir le projet
                    <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"
                      />
                    </svg>
                  </span>
                </div>
              </a>
            );
          })
        }
      </div>

      <div class="text-center mt-16">
        <a href={url('/projects')} class="btn-primary">
          Voir toutes mes réalisations
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section
    class="section-padding section-cta relative overflow-hidden section-lazy"
    aria-labelledby="cta-heading"
  >
    <div class="container-custom text-center" data-gsap="cta">
      <h2 id="cta-heading" class="section-title text-white mb-8">
        Envie d'en savoir <span class="text-accent-400 squiggle squiggle-accent">plus</span> ?
      </h2>
      <p class="text-gray-300 text-xl mb-12 max-w-2xl mx-auto leading-relaxed">
        Un projet en tête, une question, ou juste envie de discuter ? Je suis là.
      </p>
      <div class="flex flex-wrap justify-center gap-4 sm:gap-6 md:gap-8">
        <a
          href={url('/contact')}
          class="btn-chunky bg-white text-dark-900 border-dark-900 hover:bg-gray-100"
          >Me contacter</a
        >
        <a
          href={url('/about')}
          class="btn-chunky bg-transparent text-white border-white hover:bg-white hover:text-dark-900"
          >En savoir plus sur moi</a
        >
      </div>
    </div>
  </section>
</BaseLayout>
